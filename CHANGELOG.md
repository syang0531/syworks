# 변경 내역

릴리스 워크플로가 `## <버전>` 구간을 그대로 뽑아 CurseForge 변경 내역과 GitHub Release 본문으로 씁니다.
버전을 올릴 때 **맨 위에** 새 구간을 추가하세요.

## 1.0.0

**첫 정식 버전.** 0.4.0에서 세운 모양(새 아이템 0개, 공정별 기계 셋)이 그대로 맞다고 보고 1.0을 붙인다.
기계는 늘지 않았다 — **받는 것이 늘었고, 26.2에서 놓치고 있던 구멍을 메웠다.** 월드는 호환된다.

- **분쇄기가 받는 것이 101종 → 199종이 됐다.**
  - **사암·붉은 사암** 20종 → **모래·붉은 모래.** 0.4.0에서 "사암은 모래로 만든 것이라 되돌릴 이유가 없다"고
    뺐는데, 그건 제작한 사암 얘기였다. 플레이어가 만나는 사암의 대부분은 사막 지하의 자연 생성층이거나
    사원에서 뜯어온 계단이다. 붉은 모래는 배들랜드 밖으로 나를 방법이 이것 말고 없다
  - **테라코타 33종 + 벽돌 블록** → **점토.** 테라코타는 구운 점토 블록이고 벽돌은 구운 점토 덩이다.
    블록 하나가 점토 4개짜리라 1개만 돌려준다
  - **네더 벽돌·붉은 네더 벽돌** → 자갈. 요새를 뜯으면 스택으로 나온다
  - **황·진사** → 자갈. 26.2에 추가된 자연 생성 암석인데(`overworld_carver_replaceables`에 들어 있다)
    0.4.0이 통째로 놓치고 있었다
  - **돌·윤나는 흑암 버튼과 압력판** → 자갈. 던전에서 나오는데 빠져 있었다
- **숯가마가 선반 9종을 받는다.** 26.2에 추가된 블록인데 태그 어디에도 없었다.
  **벗긴 통나무 6 → 선반 6**이라 선반 하나가 통나무 하나다. 그래서 숯 **2** — 판자 등급에 넣으면
  모드가 스스로 정한 "통나무 환산값 × 2"를 어기게 된다
- **배소로의 이름을 광석 화덕으로 바꿨다.** 배소(焙燒)가 실제 야금 용어이긴 해도 너무 어려웠다.
  한글 표시명만 바뀌고 블록 id는 `ore_roaster` 그대로라 **월드에 영향이 없다**
- 복제 구멍 점검: **네더 벽돌 울타리는 슬래브 쪽(2개당 1)에 넣었다** — 블록 4 + 벽돌 2로 울타리 6이 나와서
  1:1로 주면 4:3으로 늘어난다. 사암 슬래브·벽돌 슬래브도 전부 2개당 1

## 0.4.0

**이전 버전과 완전히 다른 모드가 된다. 이름도 바뀌었다: SY Alchemy → SY Works** (modid `syalchemy` → `syworks`).
0.3.0 이전 월드와 호환되지 않는다.

- **금속 10종·합금 10종·장비 90종·합금로를 전부 삭제했다.** 합금 장비는 힘이라는 하나의 축에서
  바닐라와 정면으로 경쟁했고 거기엔 이길 수 있는 칸이 없었다 — 강철(내구 1400·공격 7.0)은
  다이아(1561·7.0)보다 약간 나쁜데 제작 단계는 다섯 배였다. 진단은 `docs/정체성-재설계.md`
- **새 아이템을 하나도 만들지 않는 모드가 된다.** 바닐라 아이템이 들어가서 바닐라 아이템으로 나온다
- **기계가 공정별로 나뉜다** — 광석은 굽고(배소로), 돌은 부수고(분쇄기), 나무는 숯으로(숯가마).
  바닐라가 화로 옆에 훈연기·용광로를 둔 것과 같은 방식이라 표를 외울 필요가 없다
- **분쇄기를 넣었다.** 암석·계단·담장·슬래브 101종을 받아 `자갈 → 모래`로 내린다.
  응회암은 점토(화산재가 변질되면 실제로 벤토나이트가 된다), 방해석·점적석은 뼛가루(석회 비료), 흙은 모래
- **모든 산출이 경험치를 준다.** 바닐라 화로처럼 **꺼낼 때** 지급한다 — 호퍼로 빼면 경험치는 안 나온다
- **숯가마를 넣었다.** 바닐라 화로가 거부하는 나무 제품(판자·계단·울타리·문·표지판·슬래브·막대기·사다리 93종)을
  받고, 통나무도 받아 **화로의 2배**를 준다. 무엇을 넣든 "통나무 환산값 × 2"라 쪼개 넣어 이득 보는 길이 없다
- **배소로를 넣었다.** 추출로를 갈아치운 것으로, **섬손으로 캔 광석만** 받는다.
  산출은 전부 **행운 III 평균보다 낮다** — 확정이고 연료·시간이 들지만 운에 기대지 않는, 상위 호환이 아닌 대안이다.
  바닐라에서 섬손은 채굴 수율 면에서 순수한 손해라 아무도 안 골랐는데, 이제 행운이냐 섬손이냐가 진짜 갈림길이 된다.
  **고대잔해는 제외했다** — 행운이 안 먹히는 광석이라 어떤 보너스든 네더라이트 게이트에 대한 순수 이득이 된다

## 0.3.0

게임을 너무 쉽게 만들던 축을 들어내고, 마법을 독립 모드로 분리했다. 같이 플레이해 보니 이 모드가 바닐라와 같이 켠 모드들의 보상을 전부 무의미하게 만들고 있었다. 전체 진단과 앞으로의 방향은 `docs/정체성-재설계.md`.

- **마법이 전부 빠졌다.** 룬 제단·룬·지팡이 18종·주문서 10종·스태프 전용 인챈트 3종 삭제. 지팡이 마법은 별도 모드로 옮겼고, 거기서는 바닐라 재료만 쓴다
- **바닐라 마법책 42종 제작 삭제** (레시피 125개). 확정 제작이 마법 부여대·주민 거래·상자 보상을 통째로 무의미하게 만들었다. 마법책은 다시 바닐라 방식으로만 구한다
- **최상위 2티어 삭제**: 초경합금(내구 4064)·백금 초합금(4600). 네더라이트의 2배가 넘는 장비가 바닐라와 던전 보스 보상을 덮어썼다. **백금**도 함께 삭제(고대잔해 추출)
- **0.2.0 월드와 호환되지 않는다.** 삭제된 아이템·블록은 월드에서 사라진다. 새 월드 권장
- 남은 것: 추출로·합금로, 금속 10종·합금 10종, 합금별 도구 5종·방어구 4종. 합금 재편(수평화)과 조각 경제는 다음 작업

## 0.2.0

- **이름이 바뀌었다: Yame → SY Alchemy** (modid `yame` → `syalchemy`). Stan Yang의 모드 가족(`sy…`)으로 묶기 위해서다
- **Minecraft 26.2 / NeoForge 26.2 / Java 25로 이동.** 1.21.1은 더 이상 지원하지 않는다
- **0.1.0 월드와 호환되지 않는다.** 블록·아이템·레시피가 전부 새 id로 옮겨갔고, 도구·방어구·레시피의 저장 형식도 26.2 것으로 바뀌었다. 0.1.0으로 시작한 월드는 새 월드에서 시작할 것
- 이름을 빼면 내용은 0.1.0과 같다: 추출로·합금로·룬 제단, 금속 11종·합금 12종·장비 풀세트, 마법책 42종 + 스태프 18종·마법 10종·스태프 전용 인챈트 3종

## 0.1.0

첫 릴리스 (Minecraft 1.21.1 / NeoForge, 이름은 Yame).

- 추출로: 바닐라 자연 블록 → 순수 금속 11종
- 합금로: 금속 2–3종 → 합금 12종
- 합금별 도구 5종·방어구 4종
- 룬 제단: 바닐라 마법책 42종 제작·강화, 룬
- 스태프 18종·마법 10종(내장 파이어볼트), 모루로 주문서 결합, 숫돌 초기화, 스태프 전용 인챈트 3종
